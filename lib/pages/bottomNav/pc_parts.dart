import 'package:flutter/material.dart';

class PcParts extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
          'PC Parts',
          style: TextStyle(
            fontSize: 20.0,
          ),
        ),
      ),
    );
  }
}
