import 'package:flutter/cupertino.dart';

class ClientsBuild extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
          'Client',
          style: TextStyle(
            fontSize: 20.0,
          ),
        ),
      ),
    );
  }
}
